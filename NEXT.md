# Next

## douglasadolph.com

The consulting site is finished and live. Landing, About, Consulting, AI, Blog
placeholder, Search. Everything below is waiting on Douglas, not on code.

- **Google Search Console.** Never submitted. The site is technically clean (no noindex,
  permissive robots.txt, sitemap with all 8 URLs, canonical tags on previews) but Google
  has not been told it exists. Needs an interactive login, so it cannot be done for him.
- **Inbound links.** Nothing points at the domain. LinkedIn and the GitHub profile are the
  natural sources once the account flag clears.
- **Blog posts.** He writes these himself. Send them over and they go up.
- **Contact address** is `consulting@douglasadolph.com` sitewide, aliased to
  `douglas@kordloom.com`. KordLoom LLC remains named as the contracting entity only.

## GitHub flag on dcadolph

Account is flagged, not suspended. Support ticket open. When it clears:

- Restore the GitHub social icon in `hugo.toml`, pointing at `github.com/dcadolph`, not
  the org.
- Republish `content/projects.md` (remove `draft: true`), and reread it against the
  brand-separation rule first, since it still names KordLoom repos and links the org.
- Recheck slop-chop.com and vamoose.dev.
- Consider linking the GitHub profile from the consulting page, which would make the
  "what I have built" claims checkable.

## Housekeeping

- Purge `test+b@example.com` and `test+c@example.com` from the signup list. Created while
  testing the endpoint.
- v0.7.0 of loomseal has no attached binaries. Actions were down when it was cut.

## Business

- Stripe invoicing and contracts, for SwitchTender and for consulting.
- Launch SwitchTender Free. Move public-log anchoring from Governed into Free.
- 25 cold DMs against `examples/agent/TARGETS.md`.

## Handoff note

SwitchTender hardening and new app ideation moved to other agents as of 2026-08-09.
