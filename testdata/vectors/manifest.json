{
  "description": "LoomSeal v0.1 conformance vectors. Each entry declares whether the bundle must verify, and when it must not, which check fails and why. A verifier is conformant when it agrees with every entry.",
  "vectors": [
    {
      "name": "anchor-declared-head-proof",
      "file": "anchor-declared-head-proof.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "An rfc3161 proof on a declared head beyond the bundled claims is not opened and earns no anchored level, because the link it attests is tied to nothing the verifier confirmed."
    },
    {
      "name": "anchor-matches-nothing",
      "file": "anchor-matches-nothing.loomseal.json",
      "must_verify": false,
      "failing_check": "anchor",
      "why": "An anchor whose coordinates match nothing in the bundle fails."
    },
    {
      "name": "anchor-only-declared-head",
      "file": "anchor-only-declared-head.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "An anchor matching only a head beyond the claims is reported but not counted as anchored."
    },
    {
      "name": "attestation-keyid-mismatch",
      "file": "attestation-keyid-mismatch.loomseal.json",
      "must_verify": false,
      "failing_check": "attestation",
      "why": "A counterparty attestation whose key_id is not the digest of its public key is refused, so it cannot misname its signer."
    },
    {
      "name": "attestation-role-swapped",
      "file": "attestation-role-swapped.loomseal.json",
      "must_verify": false,
      "failing_check": "attestation",
      "why": "A counterparty attestation whose role was changed after signing no longer verifies, because the counter-signature binds the role as well as the link."
    },
    {
      "name": "attested-claim",
      "file": "attested-claim.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A loomseal-chain-v1 claim carrying a counterparty attestation over its link verifies; the counter-signature is checked against the signer's own key and reported as vouched."
    },
    {
      "name": "chain-broken-prev",
      "file": "chain-broken-prev.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A prev link that does not match the prior link breaks continuity."
    },
    {
      "name": "duplicate-top-level-key",
      "file": "duplicate-top-level-key.loomseal.json",
      "must_verify": false,
      "failing_check": "parse",
      "why": "A repeated object key has no canonical form and is rejected."
    },
    {
      "name": "evidence-referenced",
      "file": "evidence-referenced.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "Evidence not supplied to the verifier is referenced, never counted as verified."
    },
    {
      "name": "foreign-install-linear",
      "file": "foreign-install-linear.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A loomseal-chain-v1 chain whose params.install_id is not the producer's is refused, which is what stops one install from restating another's producer block over its links."
    },
    {
      "name": "head-behind-claims",
      "file": "head-behind-claims.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A head sequence behind the newest claim is a broken chain."
    },
    {
      "name": "head-wrong-link",
      "file": "head-wrong-link.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A head at the newest sequence must carry that claim's link."
    },
    {
      "name": "keyed-structural",
      "file": "keyed-structural.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (structural)",
      "why": "A keyed chain verifies as structural: continuity holds, links are not recomputed."
    },
    {
      "name": "lone-high-surrogate",
      "file": "lone-high-surrogate.loomseal.json",
      "must_verify": false,
      "failing_check": "parse",
      "why": "A lone high surrogate escape is invalid and is rejected, not coerced."
    },
    {
      "name": "lone-low-surrogate",
      "file": "lone-low-surrogate.loomseal.json",
      "must_verify": false,
      "failing_check": "parse",
      "why": "A lone low surrogate escape is invalid and is rejected, not coerced."
    },
    {
      "name": "merkle-bad-inclusion-path",
      "file": "merkle-bad-inclusion-path.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "An audit path with a substituted sibling does not fold to the root the head names."
    },
    {
      "name": "merkle-claim-altered",
      "file": "merkle-claim-altered.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A claim edited after the tree was built no longer hashes to its own leaf, so the leaf does not recompute."
    },
    {
      "name": "merkle-claim-with-prev",
      "file": "merkle-claim-with-prev.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A tree claim carrying a previous link implies a linear chain that is not being verified."
    },
    {
      "name": "merkle-consistency",
      "file": "merkle-consistency.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (tree of 6, append-only from 4)",
      "why": "A consistency proof from an earlier root verifies, proving the log grew by appending only, which a hash chain cannot state on its own."
    },
    {
      "name": "merkle-consistency-power-of-two",
      "file": "merkle-consistency-power-of-two.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (tree of 9, append-only from 8)",
      "why": "A consistency proof whose earlier size is a power of two verifies. That case seeds the old root rather than reading it from the proof, and an implementation that expects it in the proof rejects a valid bundle."
    },
    {
      "name": "merkle-consistency-rewritten",
      "file": "merkle-consistency-rewritten.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A log that changed an entry it had already published cannot produce a consistency proof from the root the world saw before the change. Every disclosed leaf here still recomputes, so only the append-only check catches it."
    },
    {
      "name": "merkle-foreign-install",
      "file": "merkle-foreign-install.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A tree whose leaves bind an install other than the producer's is refused, which is what stops one producer from re-signing another's log and anchor."
    },
    {
      "name": "merkle-right-edge",
      "file": "merkle-right-edge.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (tree of 9)",
      "why": "A leaf on the right edge of a nine leaf log verifies, where the tree is least regular and a fold that mishandles the odd node fails."
    },
    {
      "name": "merkle-seq-past-tree",
      "file": "merkle-seq-past-tree.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A claim naming a position past the tree size is outside the log the head attests."
    },
    {
      "name": "merkle-single-leaf",
      "file": "merkle-single-leaf.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (tree of 1)",
      "why": "A log of one leaf verifies with an empty audit path, which is the correct and only path at that size."
    },
    {
      "name": "merkle-sparse",
      "file": "merkle-sparse.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (tree of 6)",
      "why": "A tree bundle disclosing two non-contiguous leaves of a six leaf log verifies, because each one folds through its audit path to the root the signed head names."
    },
    {
      "name": "merkle-wrong-leaf-index",
      "file": "merkle-wrong-leaf-index.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A claim moved to another position does not prove membership at that position."
    },
    {
      "name": "non-integer-number",
      "file": "non-integer-number.loomseal.json",
      "must_verify": false,
      "failing_check": "parse",
      "why": "A fractional number is outside the integer profile and is rejected."
    },
    {
      "name": "noncontiguous-seq",
      "file": "noncontiguous-seq.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "Claims must be contiguous in sequence; a gap is a broken chain."
    },
    {
      "name": "number-exceeds-2p53",
      "file": "number-exceeds-2p53.loomseal.json",
      "must_verify": false,
      "failing_check": "parse",
      "why": "An integer beyond 2^53 does not round-trip through an IEEE double and is rejected."
    },
    {
      "name": "producer-keyid-mismatch",
      "file": "producer-keyid-mismatch.loomseal.json",
      "must_verify": false,
      "failing_check": "signature",
      "why": "The producer key_id must be the digest of the embedded public key."
    },
    {
      "name": "raw-invalid-utf8",
      "file": "raw-invalid-utf8.loomseal.json",
      "must_verify": false,
      "failing_check": "parse",
      "why": "Invalid UTF-8 input is rejected, not coerced to the replacement character."
    },
    {
      "name": "signature-alg-rewritten",
      "file": "signature-alg-rewritten.loomseal.json",
      "must_verify": false,
      "failing_check": "signature",
      "why": "alg is outside the signed bytes, so a verifier rejects a foreign value rather than dispatching on it."
    },
    {
      "name": "signed-chained-anchored",
      "file": "signed-chained-anchored.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full), anchored by reference",
      "why": "An anchor whose coordinates match the verified head earns the anchored level."
    },
    {
      "name": "signed-chained-full",
      "file": "signed-chained-full.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "An unkeyed loomseal-chain-v1 chain with the head tied to the newest claim."
    },
    {
      "name": "signed-only",
      "file": "signed-only.loomseal.json",
      "must_verify": true,
      "level": "signed",
      "why": "A bundle with no chain reaches the signed level and no further."
    },
    {
      "name": "span-false-count",
      "file": "span-false-count.loomseal.json",
      "must_verify": false,
      "failing_check": "span",
      "why": "A count that does not match the sequence numbers is a signed false statement and fails."
    },
    {
      "name": "span-gap",
      "file": "span-gap.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full), anchored by reference, spanned",
      "why": "Beats further apart than the declared cadence are a reported gap, never a hidden one and never a failure."
    },
    {
      "name": "span-mid-adoption",
      "file": "span-mid-adoption.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full), anchored by reference, spanned",
      "why": "A chain adopting the profile mid-life attests its entire prior population at beat 1."
    },
    {
      "name": "span-missing-beat",
      "file": "span-missing-beat.loomseal.json",
      "must_verify": false,
      "failing_check": "span",
      "why": "A beat number that skips is a deleted window and fails."
    },
    {
      "name": "span-valid",
      "file": "span-valid.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full), anchored by reference, spanned",
      "why": "Two beats whose counts recompute from the sequence numbers earn the spanned level."
    },
    {
      "name": "swatch-attested",
      "file": "swatch-attested.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A claim that is both selectively disclosable and counter-signed verifies, since disclosures and attestations are independent members outside the link."
    },
    {
      "name": "swatch-foreign-disclosure",
      "file": "swatch-foreign-disclosure.loomseal.json",
      "must_verify": false,
      "failing_check": "disclosure",
      "why": "A disclosure for a field absent from the committed _sd set matches no digest and fails, so a holder cannot invent a field the producer never committed."
    },
    {
      "name": "swatch-forged-value",
      "file": "swatch-forged-value.loomseal.json",
      "must_verify": false,
      "failing_check": "disclosure",
      "why": "A disclosed field whose value was changed after signing no longer hashes to its committed digest, so selective disclosure fails while the signature and chain still verify."
    },
    {
      "name": "swatch-fully-redacted",
      "file": "swatch-fully-redacted.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A claim that discloses none of its redactable fields still verifies; the _sd set is committed by the link and nothing is revealed."
    },
    {
      "name": "swatch-selective",
      "file": "swatch-selective.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A loomseal-chain-v1 claim commits three redactable fields as an _sd set and discloses two; each disclosure hashes to a committed digest and the withheld field stays sealed."
    },
    {
      "name": "swatch-wrong-profile",
      "file": "swatch-wrong-profile.loomseal.json",
      "must_verify": false,
      "failing_check": "disclosure",
      "why": "Selective disclosure on switchtender-audit-v1 is refused, because that profile's fixed-field link does not commit the _sd set."
    },
    {
      "name": "switchtender-audit",
      "file": "switchtender-audit.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "The shipped SwitchTender profile recomputes every link."
    },
    {
      "name": "switchtender-audit-anchored-proof",
      "file": "switchtender-audit-anchored-proof.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full), anchored (proof verified)",
      "why": "An rfc3161 anchor carrying a real timestamp token verifies offline against the link it attests to, with no network and no trust in the producer."
    },
    {
      "name": "switchtender-audit-bound-install",
      "file": "switchtender-audit-bound-install.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A switchtender-audit-v1 entry that carries install_id folds it into its link and binds the entry to the producer, which an entry omitting it does not."
    },
    {
      "name": "switchtender-audit-json-escapes",
      "file": "switchtender-audit-json-escapes.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A recorded path carrying \u0026, \u003c, \u003e and U+2028 verifies, because the profile serializes with RFC 8785 rather than an encoder that escapes those characters for HTML."
    },
    {
      "name": "switchtender-audit-nanosecond-at",
      "file": "switchtender-audit-nanosecond-at.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A claim time carrying sub-microsecond digits verifies, because the profile hashes the stored time bytes rather than parsing and re-serializing them."
    },
    {
      "name": "switchtender-foreign-install",
      "file": "switchtender-foreign-install.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A bound switchtender-audit-v1 entry whose folded install_id is not the producer's is refused, which is what stops a published receipt from being lifted into another install's bundle."
    },
    {
      "name": "switchtender-params-disagrees",
      "file": "switchtender-params-disagrees.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A switchtender-audit-v1 chain.params.install_id that is not the producer's is refused: the param is informational and the per-claim install_id is what binds."
    },
    {
      "name": "switchtender-params-only",
      "file": "switchtender-params-only.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A switchtender-audit-v1 chain that stamps chain.params.install_id without folding it into its links verifies as pre-binding: the param is not part of this profile's link, so a producer adopting per-entry binding keeps every receipt it already published."
    },
    {
      "name": "tampered-payload",
      "file": "tampered-payload.loomseal.json",
      "must_verify": false,
      "failing_check": "signature",
      "why": "A byte changed after signing fails the producer signature."
    },
    {
      "name": "unknown-claim-type",
      "file": "unknown-claim-type.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A claim type outside the registry verifies and is noted, never failed."
    },
    {
      "name": "valid-surrogate-pair",
      "file": "valid-surrogate-pair.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "An astral character encodes as a surrogate pair and verifies."
    },
    {
      "name": "window-open-no-prev",
      "file": "window-open-no-prev.loomseal.json",
      "must_verify": false,
      "failing_check": "chain",
      "why": "A linear window opening at seq 5 with an empty prev is refused: a slice of a longer chain must link to the entry before it, or it is claiming to be unrooted at an arbitrary point."
    },
    {
      "name": "windowed-head",
      "file": "windowed-head.loomseal.json",
      "must_verify": true,
      "level": "signed, chained (full)",
      "why": "A head beyond the claims is a window; its link is unverified and it is not anchored."
    },
    {
      "name": "wrong-version",
      "file": "wrong-version.loomseal.json",
      "must_verify": false,
      "failing_check": "parse",
      "why": "A verifier speaks one format version and rejects others."
    }
  ]
}
